import React from "react";

const page = () => {
  /* 
    ! though (auth) will not affect on routing 
    ? intead of /auth/login, we can directly use /login as route
     */
  return <div>This is the Login Page</div>;
};

export default page;
