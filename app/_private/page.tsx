import React from "react";

const page = () => {
  return (
    <div>
      This is private folder made by just using the "_", if you put this folder
      in url, it throws an error
    </div>
  );
};

export default page;
