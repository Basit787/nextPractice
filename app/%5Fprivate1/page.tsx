import React from "react";

const page = () => {
  return (
    <div>
      We use %5F to make the folder visible with _, so if we add an adress with
      _private1 it doesnt throws as error, instead you reading this message
    </div>
  );
};

export default page;
